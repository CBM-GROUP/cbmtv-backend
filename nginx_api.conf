server {
    listen 80;
    server_name apicbmtv.cbmgroupco.com;

    # Optional: redirect HTTP to HTTPS (uncomment when you have SSL set up)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Websocket support (if needed in the future)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Serve static files directly if you map the volume
    # location /static/ {
    #     alias /path/to/your/project/staticfiles/;
    # }
}

# Optional: HTTPS Server Block (Requires SSL Certificates e.g., via Certbot)
# server {
#     listen 443 ssl http2;
#     server_name apicbmtv.cbmgroupco.com;
#
#     ssl_certificate /etc/letsencrypt/live/apicbmtv.cbmgroupco.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/apicbmtv.cbmgroupco.com/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
